OPENAI_API_KEY=your-openai-api-key

# Optional: pick backend without prompt (openai or a model filename from models/)
# ADS_LLM_BACKEND=openai

# Paths
# ADS_PDF_DIR=data/pdfs
# ADS_MODELS_DIR=models
# ADS_FAISS_INDEX_DIR=faiss_index
# ADS_FAISS_INDEX_DIR_LLAMA=faiss_index_llama

# Local models: put .gguf (or .bin) files in models/; each file appears as an option by its filename. Default is OpenAI.
